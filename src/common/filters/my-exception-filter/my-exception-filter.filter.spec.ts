import { MyExceptionFilterFilter } from './my-exception-filter.filter';

describe('MyExceptionFilterFilter', () => {
  it('should be defined', () => {
    expect(new MyExceptionFilterFilter()).toBeDefined();
  });
});
