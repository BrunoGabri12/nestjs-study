import { ErrorHandleInterceptor } from './error-handle.interceptor';

describe('ErrorHandleInterceptor', () => {
  it('should be defined', () => {
    expect(new ErrorHandleInterceptor()).toBeDefined();
  });
});
