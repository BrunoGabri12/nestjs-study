import { SimpleCacheInterceptor } from './simple-cache.interceptor';

describe('SimpleCacheInterceptor', () => {
  it('should be defined', () => {
    expect(new SimpleCacheInterceptor()).toBeDefined();
  });
});
